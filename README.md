# StreamX - Netflix-подобная платформа для просмотра фильмов и сериалов

Современная веб-платформа для просмотра информации о фильмах и сериалах с использованием TMDB API.

## 🎯 Особенности

- 🎬 **Каталог фильмов и сериалов** с постерами и описаниями
- 🔍 **Поиск** по названию
- 🎭 **Фильтрация по жанрам**
- 📺 **Просмотр трейлеров** на YouTube
- 👤 **Система авторизации** (localStorage)
- ⭐ **Список избранного** для каждого пользователя
- 🌓 **Тёмная и светлая темы**
- 📱 **Адаптивный дизайн** для всех устройств
- ⚡ **Плавная прокрутка** (Lenis)
- 🎨 **Современный UI** с анимациями (Framer Motion)

## 🛠 Технологии

- **Frontend**: React 19, React Router (HashRouter)
- **Стилизация**: Tailwind CSS, Shadcn UI
- **API**: TMDB (The Movie Database)
- **Анимации**: Framer Motion
- **Видео**: React Player
- **Smooth Scroll**: Lenis

## 📦 Установка и запуск локально

### Предварительные требования

- Node.js (v16+)
- Yarn

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <your-repo-url>
cd <repo-name>
```

2. Установите зависимости:
```bash
cd frontend
yarn install
```

3. Создайте файл `.env` в папке `frontend`:
```env
REACT_APP_TMDB_API_KEY=ваш_api_ключ_от_tmdb
REACT_APP_TMDB_BASE_URL=https://api.themoviedb.org/3
REACT_APP_TMDB_IMAGE_BASE_URL=https://image.tmdb.org/t/p
```

4. Получите API ключ от TMDB:
   - Зарегистрируйтесь на https://www.themoviedb.org/signup
   - Перейдите в Settings → API
   - Скопируйте API Key (v3 auth)

5. Запустите приложение:
```bash
yarn start
```

Приложение откроется по адресу `http://localhost:3000`

## 🚀 Деплой на GitHub Pages

### Шаг 1: Настройка package.json

Добавьте поле `homepage` в `frontend/package.json`:
```json
{
  "homepage": "https://ваш-username.github.io/название-репозитория"
}
```

### Шаг 2: Установка gh-pages

```bash
cd frontend
yarn add -D gh-pages
```

### Шаг 3: Добавление скриптов для деплоя

Добавьте в `scripts` в `frontend/package.json`:
```json
{
  "scripts": {
    "predeploy": "yarn build",
    "deploy": "gh-pages -d build"
  }
}
```

### Шаг 4: Настройка переменных окружения

Для GitHub Pages нужно либо:
1. Использовать публичный API ключ (безопасно для TMDB, т.к. ключ ограничен по доменам)
2. Или настроить GitHub Secrets и использовать GitHub Actions

#### Вариант 1: Прямое использование (проще)

Просто выполните деплой с вашим `.env` файлом:
```bash
yarn deploy
```

#### Вариант 2: GitHub Actions (рекомендуется)

1. Создайте файл `.github/workflows/deploy.yml`:
```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: |
          cd frontend
          yarn install
          
      - name: Build
        env:
          REACT_APP_TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
          REACT_APP_TMDB_BASE_URL: https://api.themoviedb.org/3
          REACT_APP_TMDB_IMAGE_BASE_URL: https://image.tmdb.org/t/p
        run: |
          cd frontend
          yarn build
          
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./frontend/build
```

2. Добавьте секрет в GitHub:
   - Перейдите в Settings → Secrets → Actions
   - Создайте `TMDB_API_KEY` с вашим API ключом

3. Закоммитьте и запушьте:
```bash
git add .
git commit -m "Add GitHub Actions deploy"
git push origin main
```

### Шаг 5: Настройка GitHub Pages

1. Перейдите в Settings → Pages
2. Выберите Source: "Deploy from a branch"
3. Выберите Branch: "gh-pages" и папку "/ (root)"
4. Сохраните

Ваш сайт будет доступен по адресу: `https://ваш-username.github.io/название-репозитория`

## 📖 Использование

### Навигация
- **Главная**: Трендовые фильмы и сериалы
- **Каталог**: Полный каталог с фильтрацией по жанрам
- **Мой список**: Избранные фильмы (требуется авторизация)
- **Поиск**: Поиск по названию

### Функции
- **Клик на постер** - открывает детальную страницу
- **Кнопка "+"** - добавляет в избранное
- **Кнопка "Смотреть трейлер"** - воспроизводит трейлер
- **Переключатель темы** - меняет светлую/тёмную тему

## 🎨 Дизайн

Приложение использует:
- Шрифты: Manrope (заголовки), DM Sans (текст)
- Цветовая схема: Красный акцент (#FF3B30) на тёмном/светлом фоне
- Анимации: Плавные переходы и hover-эффекты
- Адаптивность: Mobile-first подход

## 📝 API

Приложение использует [TMDB API](https://www.themoviedb.org/documentation/api) для получения:
- Списков фильмов и сериалов
- Детальной информации о контенте
- Информации о актёрах
- Трейлеров с YouTube
- Постеров и изображений

## ⚠️ Важные замечания

1. **HashRouter**: Используется для совместимости с GitHub Pages (не требует серверной маршрутизации)
2. **localStorage**: Данные пользователей хранятся локально (не синхронизируются между устройствами)
3. **Только информация**: Приложение показывает информацию и трейлеры, не предоставляет полные фильмы
4. **TMDB API Limits**: Бесплатный API имеет лимиты запросов

## 🤝 Вклад

Форки и пулл-реквесты приветствуются!

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Создано с помощью Emergent AI

---

**Примечание**: Это учебный проект, демонстрирующий интеграцию с TMDB API и современные веб-технологии.
