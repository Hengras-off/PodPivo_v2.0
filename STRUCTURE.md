# 📁 Структура проекта StreamX

## Обзор

StreamX - это полностью клиентское React-приложение для просмотра информации о фильмах и сериалах через TMDB API.

## Структура директорий

```
/app/
├── frontend/                    # React приложение
│   ├── public/                  # Статические файлы
│   ├── src/
│   │   ├── components/          # React компоненты
│   │   │   ├── AuthModal.js     # Модальное окно входа/регистрации
│   │   │   ├── Hero.js          # Главный баннер
│   │   │   ├── LoadingSpinner.js # Компоненты загрузки
│   │   │   ├── MovieCard.js     # Карточка фильма
│   │   │   ├── MovieRow.js      # Горизонтальный ряд фильмов
│   │   │   ├── Navbar.js        # Навигационная панель
│   │   │   ├── SearchModal.js   # Модальное окно поиска
│   │   │   └── ui/              # Shadcn UI компоненты
│   │   ├── contexts/            # React Context для состояния
│   │   │   ├── AuthContext.js   # Контекст авторизации
│   │   │   ├── ThemeContext.js  # Контекст темы
│   │   │   └── WatchlistContext.js # Контекст списка избранного
│   │   ├── pages/               # Страницы приложения
│   │   │   ├── HomePage.js      # Главная страница
│   │   │   ├── BrowsePage.js    # Страница каталога
│   │   │   ├── SearchPage.js    # Страница результатов поиска
│   │   │   ├── MyListPage.js    # Страница избранного
│   │   │   └── MovieDetailPage.js # Детальная страница фильма
│   │   ├── services/            # API сервисы
│   │   │   └── tmdb.js          # TMDB API интеграция
│   │   ├── App.js               # Главный компонент с роутингом
│   │   ├── App.css              # Глобальные стили
│   │   └── index.css            # Tailwind CSS и кастомные стили
│   ├── .env                     # Переменные окружения
│   ├── package.json             # Зависимости и скрипты
│   └── tailwind.config.js       # Конфигурация Tailwind
├── .github/
│   └── workflows/
│       └── deploy.yml           # GitHub Actions для деплоя
├── README.md                    # Документация проекта
└── DEPLOY.md                    # Инструкция по деплою

```

## Ключевые файлы

### Frontend Configuration

#### `/frontend/.env`
Конфигурация API ключей:
```env
REACT_APP_TMDB_API_KEY=ваш_ключ
REACT_APP_TMDB_BASE_URL=https://api.themoviedb.org/3
REACT_APP_TMDB_IMAGE_BASE_URL=https://image.tmdb.org/t/p
```

#### `/frontend/package.json`
- Зависимости проекта
- Скрипты для запуска и сборки
- Настройка homepage для GitHub Pages

#### `/frontend/tailwind.config.js`
- Кастомные цвета (brand primary/hover)
- Конфигурация темной/светлой темы
- Шрифты (Manrope, DM Sans)

### Компоненты

#### `src/components/Navbar.js`
- Навигация между страницами
- Кнопки поиска и авторизации
- Переключатель темы
- Мобильное меню

#### `src/components/Hero.js`
- Главный баннер с featured фильмом
- Кнопки воспроизведения и добавления в список
- Адаптивный дизайн

#### `src/components/MovieCard.js`
- Карточка фильма с постером
- Hover эффекты
- Рейтинг и год выпуска
- Кнопки действий

#### `src/components/MovieRow.js`
- Горизонтальная прокрутка фильмов
- Стрелки навигации
- Поддержка мыши и touch

### Контексты (State Management)

#### `src/contexts/AuthContext.js`
Управление авторизацией:
- `login(email, password)` - Вход в систему
- `signup(name, email, password)` - Регистрация
- `logout()` - Выход
- Хранение в localStorage

#### `src/contexts/ThemeContext.js`
Управление темой:
- `theme` - Текущая тема (dark/light)
- `toggleTheme()` - Переключение темы
- Автоматическое применение класса `.dark`

#### `src/contexts/WatchlistContext.js`
Управление списком избранного:
- `watchlist` - Список фильмов
- `addToWatchlist(movie)` - Добавить фильм
- `removeFromWatchlist(movieId)` - Удалить фильм
- `isInWatchlist(movieId)` - Проверка наличия
- Привязка к пользователю

### Страницы

#### `src/pages/HomePage.js`
- Hero баннер с трендовым фильмом
- Секции: Тренды, Популярные, С высоким рейтингом
- Загрузка данных при монтировании

#### `src/pages/BrowsePage.js`
- Переключатель фильмы/сериалы
- Фильтрация по жанрам
- Сетка с карточками
- Пагинация через TMDB API

#### `src/pages/SearchPage.js`
- Отображение результатов поиска
- Получение query параметра из URL
- Сетка результатов

#### `src/pages/MyListPage.js`
- Список избранных фильмов пользователя
- Проверка авторизации
- Пустое состояние

#### `src/pages/MovieDetailPage.js`
- Детальная информация о фильме
- Воспроизведение трейлера (React Player)
- Список актёров
- Похожие фильмы
- Кнопка "Назад"

### Сервисы

#### `src/services/tmdb.js`
TMDB API методы:
- `getTrending(mediaType, timeWindow)` - Трендовый контент
- `getMoviesByCategory(category, page)` - Фильмы по категории
- `getTVByCategory(category, page)` - Сериалы по категории
- `searchMulti(query, page)` - Универсальный поиск
- `getMovieDetails(movieId)` - Детали фильма
- `getTVDetails(tvId)` - Детали сериала
- `getGenres(mediaType)` - Список жанров
- `discoverMovies(filters, page)` - Поиск фильмов с фильтрами
- `discoverTV(filters, page)` - Поиск сериалов с фильтрами
- `getImageUrl(path, size)` - URL изображения
- `getTrailerUrl(videos)` - URL трейлера

## Технологии

### Core
- **React 19** - UI библиотека
- **React Router v7** (HashRouter) - Маршрутизация
- **Tailwind CSS** - Стилизация
- **Shadcn UI** - UI компоненты

### Дополнительные библиотеки
- **Framer Motion** - Анимации
- **React Player** - Видео плеер для трейлеров
- **Lenis** - Плавная прокрутка
- **Lucide React** - Иконки
- **Axios** - HTTP запросы

### Development
- **CRACO** - Конфигурация Create React App
- **ESLint** - Линтер
- **PostCSS** - CSS обработка
- **Autoprefixer** - Автопрефиксы для CSS

## Data Flow

```
User Action
    ↓
Component
    ↓
Context (if needed)
    ↓
API Service (tmdb.js)
    ↓
TMDB API
    ↓
Response
    ↓
State Update
    ↓
UI Re-render
```

## LocalStorage Schema

### Авторизация
```json
// streamx_user (текущий пользователь)
{
  "email": "user@example.com",
  "name": "Имя Пользователя"
}

// streamx_users (все пользователи)
[
  {
    "email": "user@example.com",
    "name": "Имя Пользователя",
    "password": "password123"
  }
]
```

### Список избранного
```json
// streamx_watchlist_{email}
[
  {
    "id": 12345,
    "title": "Название фильма",
    "poster_path": "/path.jpg",
    "media_type": "movie",
    ...
  }
]
```

### Тема
```
streamx_theme: "dark" | "light"
```

## Routing

### Маршруты (HashRouter)

```
/#/                  - Главная страница
/#/browse           - Каталог
/#/search?q=query   - Поиск
/#/my-list          - Мой список
/#/detail/:mediaType/:id - Детали (movie или tv)
```

## Переменные окружения

| Переменная | Описание | Пример |
|-----------|----------|---------|
| `REACT_APP_TMDB_API_KEY` | API ключ TMDB | `abc123...` |
| `REACT_APP_TMDB_BASE_URL` | Базовый URL API | `https://api.themoviedb.org/3` |
| `REACT_APP_TMDB_IMAGE_BASE_URL` | URL для изображений | `https://image.tmdb.org/t/p` |

## Сборка

### Development
```bash
cd frontend
yarn start
```
Запускается на `http://localhost:3000`

### Production
```bash
cd frontend
yarn build
```
Создаёт оптимизированную сборку в `frontend/build/`

## GitHub Actions Workflow

При push в `main`:
1. Checkout кода
2. Установка Node.js и зависимостей
3. Сборка с environment variables
4. Деплой в ветку `gh-pages`
5. GitHub Pages автоматически публикует

## Лицензия

MIT
